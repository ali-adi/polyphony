---
description: Never start a paid full tune_level run without the user confirming and reviewing first
alwaysApply: true
---

# Full recall-loop runs

Do not start `tune_level.py --on full` (discovery, verify, or `--force-full`) unless the user has confirmed that specific run after review. Propose it and wait. Tuning-subset, rescore, merge, collect, and strip are allowed under the runbook.
